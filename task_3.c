#include "shell.h"

/**
 * main - Entry point
 *
 * Return: Success
 */
int main(void)
{
	char input[MAX_INPUT_LENGTH];
	char input[MAX_INPUT_LENGTH];
	int i;
	char *token = strtok(input, " ");

	while (1)
	{
		printf("simple_shell> ");
		fflush(stdout);
		if (fgets(input, sizeof(input), stdin) == NULL)
	}
}
